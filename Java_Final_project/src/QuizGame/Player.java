package QuizGame;
// import QuizGame.User;

public class Player extends User{
	
	// fields
	int selectedAnswer; // could set it to 0 as default
	int currentBet; // could set it to 0 as default
	
	// constructor 
	public Player(String n) {
		super(n);
	}
	
	// methods
	void submitAnswer(int ans) {
		// to do: idk build the answer class ig
	}
	
}
